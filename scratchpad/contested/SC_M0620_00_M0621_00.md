# Targeted arbitration: SC_M0620_00_M0621_00

Reviewed only the accuracy-contested and literary-modified indexes `10`, `12`,
`23`, `38`, and `39` against the filtered Japanese projection, both QC reports,
and `wave200_shard50` continuity locks.

## Decisions

- `10` (`つがい`): retain `「A mating pair.」`. It preserves the zoological
  label and Sunao's immediate animal retort without adding relationship detail.
- `12` (`ビシッ！ と突っ込んでくれる。`): retain `She snapped right back.`
  The compact narration preserves the sharp tsukkomi response.
- `23` (`むっつり？`): retain `「Closet pervert?」` over `「Secret pervert?」`.
  Both are defensible, but the current question is idiomatic and remains a tease,
  not a factual accusation.
- `38` (`せいぜいこの程度よ！`): retain `「And that's as far as I go!」`.
  It preserves Sunao's claim that the wallpaper marks the limit of her excess.
- `39` (`やっぱお前俺より上だ`): retain `「...No, you've still got me beat.」`.
  It preserves Leo's comparison without changing its object or degree.

Final translation changes from arbitration: NONE. No alternative outranked the
current lines under accuracy > narrative safety > voice > naturalness.

