# Accuracy QC: SC_J0100_20_Z9999_99

All 127 permitted indexes (`1-127`) were checked independently, line by line,
against the certified filtered projection, final J preflight, and translation
handoff. No exclusion applies.

The common convergence preserves the real Nagomi as Mecha Takane's captive,
Poem's sparse non-graphic threat, the small explosion and living Nagomi's
retort, the failed-teamwork joke, and the broad comic confrontation. Generic
`Voice` remains at indexes `53-54`, and neither target line names Hinano.
Takane identifies her in dialogue at `56`, and the explicit `雛乃` speaker tag
appears at `57`. Yoshimi's colder beat at `79-81` remains unexplained, Shinichi's attempt
at `93-97` remains interrupted and non-graphic, and index `107` retains the
abrupt return to the school grounds. The unnamed younger brother at `110`,
Mana's punchline complaint at `111`, Sunao's crest catchphrase at `119`, and
the unresolved side-character ensemble ending remain intact.

## Accuracy corrections

- Index `18`
  - Before: `「She was walking to school all alone, so she was really easy to kidnap!」`
  - After: `「She was going to school all alone, so she was really easy to kidnap!」`
  - Reason: `登校してた` says Nagomi was going to school; it does not establish
    that she was walking. The correction removes an unsupported travel mode
    without changing Umi's kidnapping agency.
- Index `41`
  - Before: `「Wait. That was just the worst possible pairing!」`
  - After: `「Wait. That was just one bad matchup!」`
  - Reason: Leo says that one particular combination was bad
    (`特定の組み合わせが悪かった`); the source does not assert the superlative
    `worst possible`.
- Index `84`
  - Before: `「Y-you're Hiiragi, the famous lawyer!」`
  - After: `「Y-you're Attorney Hiiragi!」`
  - Reason: `柊弁護士` identifies Hiiragi by surname and profession/title. The
    source does not state that she is famous; Shinichi separately identifies
    himself as a fan at the next index.
- Index `117`
  - Before: `「Oh, really? Then let's just forget this whole thing and call it a day...」`
  - After: `「Oh, figures. Then let's just forget this whole thing and call it a day...」`
  - Reason: `やっぱり` confirms Leo's expectation after Igaguri's observation;
    `really?` incorrectly made him sound surprised or doubtful.

## Contested keep decisions

- Index `4`: `Poepoe` remains the direct phonetic nickname `ぽえぽえ`; the
  speaker-map identity remains `Poem`.
- Index `45`: `take out` retains the competitive claim and the dark written
  undertone of ruby-marked `殺（と）らせて` without asserting a literal killing.
- Indexes `55-56`: `Kaname-nee` and `Hinano-neesan` retain the explicit sister
  hierarchy without altering speaker-map identities.
- Indexes `66-67`: `privates` / `pride` preserves the paired malapropism and
  immediate correction without expanding it.
- Index `80`: unwrapped `Sato... san?` preserves Leo's sudden fearful formality
  and the narration/dialogue boundary.
- Index `111`: `You never landed the punchline` accurately conveys that Leo's
  recounted story still lacks an ending beat; it does not add a new event.

Result: PASS (`127/127`).

Changed indexes: `18`, `41`, `84`, `117`.

Materially defensible readings requiring arbitration: NONE.
