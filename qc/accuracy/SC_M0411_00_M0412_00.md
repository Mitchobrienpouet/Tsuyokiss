# Accuracy recertification: SC_M0411_00_M0412_00

All 53 permitted rows (`1-53`) were checked independently, line by line,
against the current overlay-aware projection. No exclusion applies to this
scene, and no excluded source row was inspected or inferred.

## Correction applied

- `31`
  - Before: `「Konoe and I have known each other forever. We just made up,
    that's all.」`
  - After: `「Konoe and I are old acquaintances. We just made up, that's all.」`
  - Reason: `昔からの知り合い` identifies a long-standing acquaintance; the
    former hyperbole could incorrectly imply lifelong childhood-friend status.
    The correction preserves Leo's deliberate minimization and reveal timing.

The prior `(smile)` display-direction decision at `44` remains correct. The
parrot's aerial observation and imitation, `Rom-Com Boy` teasing, and
Shinichi's unanimous-loss survey preserve speaker and agency. The handoff map
repair is correct: `土永さん` maps to `Mr. Tsuchinaga` at displayed rows `6,
8, 9, 17, 41, 51`.

Deterministic scene gates: **PASS** - exact indexes and join, source metadata,
engine IDs, internal file identity, speaker-map coverage, wrappers, JSON, and
CP932. This text/source review makes no runtime-rendering or scene-image claim.
