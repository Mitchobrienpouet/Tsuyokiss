# Accuracy QC: SC_M0601_00_M0602_00

All 25 permitted indexes (`1-25`) were checked independently against the
filtered Japanese projection and `wave200_shard46` continuity spec. The start
of summer vacation, Otome's teasing interpretation of the outing, Leo's
uncertainty and private disappointment, Otome's limited return to her family
home, the phone-planning recap, and the Dobuzaka meeting point preserve their
source meaning, speaker, timeline, and reveal order.

## Contested keep decisions

- Indexes `4-5`: `quite the hot item` and `seriously dated slang` remain a
  linked setup and punchline for `アツアツ` / `死語`. The tease is recognizably
  dated without confirming that Leo and Sunao are a couple or that either has
  called the outing a date.
- Index `13`: `「Mm.」` correctly preserves the clipped `ン` as a noncommittal
  sound. It does not expand Leo's response into agreement, denial, or stated
  romantic intent.

Result: PASS.

Changes: NONE.

Materially defensible alternatives: indexes `4-5` could use a deliberately
old-fashioned `lovebirds` construction, and index `13` could use `「Nn.」`;
neither is more accurate than the current text.
