# Accuracy QC: SC_E0820_00_E0830_00

PASS after adversarial source-to-target review of all 173/173 projected rows.

- Coverage exactly matches the regenerated projection. Filtered indices
  `112-125`, `136-138`, and `145` are absent; no excluded material was bridged,
  summarized, or reconstructed.
- Speaker map is exact: `レオ` -> Leo, `なごみ` -> Nagomi, `新一` -> Shinichi.
  Nagomi's direct `センパイ` remains `Senpai`; `フカヒレ` remains `Fukahire`.
- Cell-battery setup/payoff (`4-5`, `45-48`), the return time (`27-28`, `53`),
  the spare-key action and motive (`70-85`), the two-day Obon separation
  (`99-107`, `139-144`), and the arcade exchange (`183-191`) preserve agent,
  tense, sequence, and causality.
- `休憩` at `17-18` is rendered functionally without a false English kanji
  explanation. `通い妻` at `21` remains a visiting-wife observation, not a
  stronger claim that the two live together or are married.
- Accuracy review refined `16` to preserve the source's pointed `this is youth`
  phrasing and refined `186` so Shinichi's taunt does not invent wasted effort.
- JSON, exact-key coverage, duplicate-key checks, and independent CP932 encoding
  all pass. No materially defensible alternate reading remains.
