# Accuracy QC: SC_M0622_00_M0630_00

All 81 permitted indexes were checked independently against the filtered
Japanese projection and `wave200_shard50` continuity spec. Festival chronology,
hand-holding agency, Noriko/peanut-butter callbacks, the yukata comparison,
compliment timing, minor finger nicks, public-restraint logic, and the unfinished
index `81` remain source-complete.

The kiss sequence remains Sunao initiation -> Leo reciprocation -> Leo
initiation, while parenthesized indexes `58` and `60` remain internal reactions.
Index `71` retains `「Aren't you bolder than usual?」` for emphatic `ダイターン`;
no unsupported `Daitarn`, anime, robot, or other named reference is introduced.

## Correction

- Index: `36`
- Source: `「去年まではあのメンツで花火見てたけど」`
- Before: `「I used to watch the fireworks with that bunch every year.」`
- After: `「Up through last year, I watched the fireworks with that bunch.」`
- Reason: `去年までは` means "up through/until last year." The prior
  rendering added an unsupported annual frequency with `every year` and omitted
  the explicit temporal boundary.

Result: PASS after the correction above. No other hard semantic error was found.

Materially defensible alternative: index `71` could use `「Aren't you more
daring than usual?」`; the current `bolder` preserves the same playful emphasis.
