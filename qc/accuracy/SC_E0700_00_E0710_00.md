# Accuracy QC: SC_E0700_00_E0710_00

All 850 permitted rows checked against the filtered Japanese projection.

- `417`: `I will master the secret of that flavor.` -> `I will master the
  techniques behind that flavor.` Removed an unsupported "secret" and restored
  the source's cooking-technique referent.
- `477`: `Around when the Manager and I should finish our curry.` -> `Around
  when both the Manager's curry and mine should be ready.` Restored the two
  distinct curries and the completion-time meaning.
- `617`: `watch the work` -> `watch the movie`. Resolved `作品` from the local
  movie/VCR context.
- `689`: `magazine ad` -> `weekly magazine ad`. Restored `週刊雑誌`.
- `780`: `long-haired brunette` -> `girl with the long black hair`. Corrected
  `黒髪`; brunette could wrongly imply brown hair.
- `844`: `Yeah...` -> `Seriously...`. Corrected `本当` in Leo's reflective
  response rather than treating it as simple assent.

No materially defensible accuracy alternative remains.
